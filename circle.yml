machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  node:
    version: 0.12.0
  ruby:
    version: 2.3.1

test:
  override:
    - ./bin/ci

dependencies:
  override:
    - ./bin/setup
  cache_directories:
    - "node_modules"
    - "~/.rvm/gems/"
    - "~/.cache/yarn"
